{% use '@Contao/component/_headline.html.twig' %}

{% block headline_component %}
    {% if data.headlineStyle|default %}
    {% set headline = headline|merge({
        'attributes': attrs()
            .addClass(data.headlineStyle)
            .mergeWith(headline.attributes|default)
    }) %}
    {% endif %}
    {{ parent() }}
{% endblock %}