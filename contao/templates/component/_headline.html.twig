{% use '@Contao/component/_headline.html.twig' %}

{% block headline_component %}
    {% set headline = headline|default(_context)|merge({
        'attributes': attrs()
            .addClass(data.headlineStyle|default)
            .mergeWith(headline.attributes|default)
    }) %}
    {{ parent() }}
{% endblock %}